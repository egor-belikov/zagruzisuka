# Подключается после worker.env на слабом VPS (merge: docker-compose.egorvps.yml).
# ~9 GiB на один файл — запас относительно tmpfs 10G и тома filestorage.
YTDLP_MAX_FILESIZE_BYTES=9663676416
MAX_SIMULTANEOUS_DOWNLOADS=1
